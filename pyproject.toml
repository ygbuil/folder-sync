[tool.poetry]
name = "backend"
version = "0.1.0"
description = "Track differences between 2 folder and create a backup."
authors = ["Llorenç Buil Fustagueras"]
readme = "README.md"

[tool.poetry.scripts]
backend = "backend.__main__:_main"

[tool.poetry.dependencies]
python = "^3.11.6"
customtkinter = "^5.2.1"
pre-commit = "^3.6.0"
pillow = "^10.1.0"
pytest = "^7.4.3"
ruff = "^0.1.8"
click = "^8.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
